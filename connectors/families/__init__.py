"""Connector families — one declarative module per shared access pattern.

A *family* (OCDS publishers, ATS job boards, newswire RSS, SDMX agencies, the
user-defined generic-REST connector) shares one code path driven by a per-source
endpoint list + field map, as mandated by v1-definition Q12.
"""
